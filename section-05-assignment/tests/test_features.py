# import numpy as np
# from regression_model.config.core import config
# from regression_model.processing.features import ExtractLetterTransformer


def test_ExtractLetterTransformer(train_features):

    # TODO...

    # PITA as saple_input_)data has NaNs in it so needs to be transformed before
    # being passed to ExtractLetterTransformer

    assert True #np.all(ref == transformed[feature])
